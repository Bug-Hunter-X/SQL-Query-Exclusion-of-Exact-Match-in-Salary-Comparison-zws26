```sql
SELECT * FROM employees WHERE department = 'Sales' AND salary >= 100000;
```
This revised query uses the `>=` (greater than or equal to) operator to correctly include employees with salaries of exactly 100000.